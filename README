Run GFS control forecast with replay to NOAA Eagle Solo (graphcast).

1) run first 6-h cold start forecast (save restart 3-h into forecast).
    * run setup_rundir.sh with export do_iau=NO.
    * sbatch run_forecast.sh to run 10 day forecast.
    * copy FV3_RESTART and FV3ATM_OUTPUT to *_control.
2) get graphcast forecasts using get_graphcast_forecast.sh.
3) run setup_expdir.sh, then sbatch run_replay_forecast.sh.
